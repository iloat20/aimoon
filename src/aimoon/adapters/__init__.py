"""适配器层 (Adapters Layer)

六边形架构中的适配器层，负责连接核心领域与外部世界。

职责：
- 将外部输入转换为核心领域能理解的格式（驱动适配器）
- 将核心领域的输出转换为外部系统能理解的格式（被驱动适配器）
- 实现应用层定义的端口接口
- 隔离核心业务逻辑与外部技术细节

架构位置：
- 位于应用层外围
- 分为 driving（输入/驱动）和 driven（输出/被驱动）两大类
- 对应六边形架构的接口适配器层

依赖规则：
- 可以依赖 application 层和 domain 层
- 不能依赖具体的外部框架（那是框架层的事）
- 必须通过端口与核心交互
"""
