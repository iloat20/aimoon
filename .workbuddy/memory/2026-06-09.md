# 2026-06-09 工作日志

## 完成：深入审查并修复因子过多和模型训练问题

### 审查发现
- 系统有 457 个 Alpha Zoo 因子（Alpha101 + GTJA191 + Qlib158 + 学术6 + 专有5）
- 训练时`_select_factor_subset()`只随机抽样50个，导致训练不稳定、推理特征不匹配
- `trainer.py`有死代码（return后不可达的8行代码）
- 训练日期选择使用`np.random.choice()`（随机），训练结果不可复现
- 回测引擎一次性计算全部457个因子，大量浪费
- per-bar技术指标(TechInd)重复构建，没有缓存

### 修复内容

#### 1. 新建 `src/aimoon/ml/factor_quality.py` — 因子质量前置过滤系统
- 使用 ICIR + Turnover + Correlation 三网关过滤
- 结果持久化到 `.aimoon_cache/factor_quality/filtered_factor_ids.json`
- 训练和回测/筛选时共用同一过滤后的因子列表

#### 2. 修改 `src/aimoon/ml/feature_pipeline.py`
- `_select_factor_subset()` 改为优先使用质量过滤后的因子列表
- 缓存不存在时回退到分组等比抽样（兼容首次运行）

#### 3. 修改 `src/aimoon/ml/trainer.py`
- **删除死代码**（lines 211-218，return后不可达）
- 移除未使用的 `rng` 变量
- 训练日期选择从随机抽样改为确定性均匀分布

#### 4. 修改 `src/aimoon/screener.py`
- 因子质量过滤改为使用 `get_or_compute_filtered_ids()` 缓存
- 避免重复计算 ICIR/turnover/correlation

#### 5. 修改 `src/aimoon/enhanced_backtest.py`
- 只计算质量过滤后的因子，而非全部457个
- 添加 per-bar TechInd 缓存（`_bar_ti_cache`），每个bar开始时重置
- `_score_stock()` 和 `_phase2_momentum_check()` 共用缓存

### 后续优化 — 回测审查 + 优化报告

1. **回测过程审查**：尝试运行 `aimoon backtest`，发现耗时过长（网络数据获取超时）
2. **分析已有回测数据**：
   - 检查了 `output/backtest_summary.md`、`training_output.log`、`output/ 下 CSV`
3. **关键发现**：
   - **IC 异常高**（0.92）— 超出金融数据合理范围（0.03~0.15），疑似训练管线有前瞻偏差
   - **Sharpe 14.04**、年化 +541%、最大回撤 2.29% — 全部超出常识范围
   - 长期回测（850天）0 笔交易 — 回测对历史长度极度敏感
   - 仅 81 只股票做 ML 训练，严重不足
4. **已生成优化报告**：`output/optimization_report_20260609.md`
5. **新增 CLI 命令**：`aimoon cache clear-factors` — 手动清除因子白名单缓存
6. **因子质量缓存 TTL 改为 30 天**（用户要求）

### 后续修复 — 用户确认的三项修复

#### 修复 2: Purged CV 加强
- `trainer.py` — XGBoost/ElasticNet: n_splits 5→8, embargo forward_days→forward_days*2
- `lgbm_trainer.py` — LightGBM: 同上

#### 修复 3: ML 预测失败回退机制
- `enhanced_backtest.py` — `_get_ml_scores_for_date()` 所有 return None 改为 fallback
- 新增 `_get_fallback_ml_scores()` — 用 Alpha Zoo 因子聚合得分排名替代 ML 分数
- `_compute_alpha_signals()` — 保存 `_alpha_signals_ctx` 供 fallback 使用

#### 修复 4: 特征维度对齐验证
- `trainer.py` — 训练完成后保存 `canonical_feature_names.json`
- `ensemble.py` — `from_cache()` 优先加载规范特征名
- `ensemble.py` — `predict()` 增加特征维度对齐验证（>50%缺失报 warning）

### 回测分析 + 优化建议
- 分析了 `output/backtest_report_20260608_234623.md` 的 7 笔交易回测结果
- **核心问题**：交易次数太少（7 笔/200 天），年化仅 2.29%
- 生成优化建议报告 `output/optimization_recommendations_20260609.md`
- 推荐 P0 优化：entry_threshold 从 60→40，max_positions 从 2→5，stop_loss 从 4%→7%

### Alpha360 向量化 + 回测主循环优化
1. **Alpha360 全向量化重构**（`ml/alpha360.py` v2）
   - 消除 per-stock Python 循环，改用 numpy 矩阵运算
   - 81 只股票 × 60 天：~7ms/次（原来 ~1s/次），**140x 提升**
   - 200 次训练调用从 ~200s 降至 ~1.4s

2. **回测 Phase 4 性能优化**（`enhanced_backtest.py`）
   - 只评估 ML 排名前 30 的候选股（原 81 只）
   - ML 分数 < 30 快速跳过
   - 5 日涨幅 < -5% 快速跳过
   - 修复了 `_ti_cache` 变量未定义的 bug

### 瓶颈定位 + 因子缓存优化（6/9）
1. **定位瓶颈**：NaN 警告日志（1288 行/回测）+ `_get_ml_scores_for_date` 每次 310ms（重建 Alpha Zoo 因子）
2. **NaN 警告去重**（`factors/registry.py`）：`_nan_warned` 集合，每因子只警告一次
3. **新增 `_extract_features_cached()`**（`enhanced_backtest.py`）：复用 `_factor_cache`，Alpha Zoo 因子不重算，只计算 Alpha360（7ms）+ 技术特征（~11ms），合计 **~18ms/次（17x 加速）**
4. **新增 `EnsemblePredictor.has_any`** 属性
5. **回测结果**（81 只，500 天，2025/3~2026/6）：**57.6 秒**，总收益 +34.25%，年化 27.15%，Sharpe 6.67，胜率 58.82%，17 笔交易
