# 2026-07-07 工作日志

## 任务1：aimoon 项目优化（P0–P3）收尾
- 完成 P2-a：重复 XML 剥离逻辑合并到 `ai/xml_utils.py`（analyzer.py / orchestrator.py 改为 import `strip_xml_tool_calls`）。
- 完成 P2-c：6 个工具 `run` 统一用 `@tool_safe` 装饰器（`tools/_safe.py`），去除各自外层 try/except。
- 完成 P3#15：删除 60s `tool_cache.py` 缓存层（key 不含 web-search 输出→peer 数据陈旧），`_fingerprint` 迁到 `ai/cache.py`。
- 完成 P3#16：ANALYSIS 空输出重试仅捕传输层异常 → 扩展为含 `httpx.HTTPStatusError`（见 BUG-3）。
- 完成 P3#17：guba 采集器 `wait_for_timeout(3000)` 改为 `wait_for_selector('tr.listitem')` + try/except 兜底。
- 完成 P3#18：新增 `tests/test_tool_chain.py`（6 工具 run(None) 均返回 dict 含 `__partial__`）；移除过时的 cache-hit 测试。
- 完成 P3#19：`.gitignore` 加 `.pytest-tmp/`；删除两个根目录扁平化 junk 目录。
- 修复 `tests/test_integration_pipeline.py`：原 `--use-v2` 已移除（v2 为默认，`--legacy` 退出），改写 4 个陈旧 CLI 测试 + 冒烟测试改 opt-in 跳过。
- 校验：ruff 通过、mypy 通过、pytest 111 passed / 1 skipped。

## 任务2：深度 Bug 审计（/using-superpowers 触发）
- 基线已绿后做广度审查，输出 `docs/bug_audit_2026-07-07.md`。
- 中危 3 个：
  1. K 线成交量三档单位不一致（档1/2 手、档3 股×100）→ 绝对量差 100×。
  2. 财务时序表 EPS 列恒为 N/A（`_serialize` 漏 emit `eps`，实体有该字段）。
  3. LLM 瞬时 HTTP 429/500 未重试（`HTTPStatusError` 不在重试 except 元组）。
- 低危 2 个：peer_compare 逻辑重复 + `TOOL_RUNNERS["peer_compare"]` 死代码；AGENTS.md 引用已删除的 scoring.py / data_cleaner.py。
- 排除多项疑似 bug（research/social_posts default_factory 非 None、integrity_checker 源匹配、history_financial 已填充、工具入参顺序对齐、table_renderer/phases 守卫完善）。

## 项目约定（长期）
- K 线 canon 单位 = **手**（经核实）：报告成交量图 `report/index.html` 按 ÷10000 标「万手」；档1/2 akshare 本就为手；档3 腾讯原 `×100`(手→股) 已改为 `vol_f` 直接用手。AGENTS.md 原"Tencent volume = 手 ×100"条目已同步更正。
- AGENTS.md 仍过时（低危待清）：引用了已删除的 `scoring.py`（实际在 `validation/integrity_checker.py`）与 `data_cleaner.py`。

## 任务3：修复 3 个中危 bug（用户批准"可以"后落地）
- BUG-3（`orchestrator.py:286`）：ANALYSIS 重试 except 元组加入 `httpx.HTTPStatusError`，瞬时 429/500 现重试至多 3 次（原直接穿透→静默空分析）。
- BUG-2（`financial_temporal.py:57-58`）：`_serialize` 补 `"eps": f.eps` 与 `"bvps": f.bvps`，EPS 列不再恒 N/A（渲染层 `y.get("eps")` 已支持）。
- BUG-1（`kline.py:194`）：档3 腾讯 `volume=vol_f`（移除 ×100），三档统一为手，与报告万手图一致。AGENTS.md 对应条目更正。
- 回归测试：新增 `test_orchestrator_wiring.py::test_analysis_retries_on_http_status_error`、`test_tool_chain.py::test_financial_temporal_emits_eps_and_bvps`、`test_tool_chain.py::test_kline_tencent_volume_in_shares_not_x100`。
- 校验：ruff 通过、mypy 通过、pytest **114 passed / 1 skipped**（较修复前 +3，即 3 个新回归测试）。
- 低危 BUG-4/BUG-5 保留未动（peer_compare 重复逻辑；AGENTS.md 过时引用）。

## 任务4：AI 流程 + 报告质量优化（Tasks #1–#5 验证收尾）
- 代码改动（前序 session 完成，本轮验证）：
  - `orchestrator.py`：COMPILE reasoning_effort `"max"→"high"`；新增 `_stream_llm_content`(流式+2次传输重试) 与 `_collect_content_stream`(SSE 实时打印 `##` 章节)；COMPILE 空输出标记 `partial` 不再静默回退 draft；`_run_pipeline` 末尾若 COMPILE 未产出但有 ANALYSIS 初稿则以其为终稿(含真实数字)避免降级到 legacy 空话。
  - `compile.md`：注入 `{{ self_check_fixes }}`（修复 SELF_CHECK 失效 bug）+ 约束逐条回应修正。
  - `generator.py`：新增 `_build_ai_sections(report_text)` 按 `## ` 拆结构化章节(引言→报告摘要,TOC 锚点)；`_build_context` 注入 `ai_sections`/`report_meta`(chars/degraded/engine)。
  - `index.html`/`style.css`：AI 报告卡片加降级 badge、`.report-degraded` 横幅、`.report-meta` 元信息条、`.report-toc` 目录、`.ai-section` 分节;无 `## ` 时回退原样渲染。
- 本轮修复 2 个遗留问题：
  - **F821**：`_build_context` 引用未定义 `settings.deepseek_model` → 改为由 `generate()` 传参 `settings`(原 `get_settings()` 已在 generate 内调用)。
  - **mypy**：`_collect_content_stream(resp: object)` → `resp: httpx.Response`（文件已 import httpx）。
- 校验：ruff 通过、mypy 通过（2 文件）、pytest 全绿（含新增 `tests/test_report_structure.py` 6 项：_build_ai_sections 拆分/转义 + 结构化/扁平渲染路径）。
- 行为要点（长期）：COMPILE 失败不再降级到 legacy 空话，而是用 ANALYSIS 初稿保底；报告含 `## ` 标题才结构化(TOC+分节)，否则整体原样。

## 任务5：用户反馈"AI 报告明显不行" → 根因诊断 + 修复
- 根因澄清：用户看到的是 (a) 我演示用的**手写占位文本**(非真实分析) 或 (b) `--mock` 跑出的 legacy mock 空话(`mock.py:mock_analysis_report` 原产出"一、公司概况…财务状况良好…中性持有"275 字空话)。**真实 v2(有 Key)产出质量不差**——缓存 `cache/ai_response_5min/1bc49e29…json`(7/6) 为格力电器真实草稿,含硬数字(营收1711.2亿/ROE19.3%/护城河论证)。
- 判定:quick-start 推荐 `aimoon <code> --mock`(无 Key),所以**任何无 Key 用户第一次见到的就是那坨垃圾**——是真问题。
- 修复1(`src/aimoon/adapters/driven/ai/pipeline/prompts/compile.md`):删除重复的 `{{ self_check_fixes }}` 占位符(原 11-12 行与 14-15 行重复,之前补注入时多写一遍)。
- 修复2(`src/aimoon/adapters/driven/collectors/mock.py:mock_analysis_report`):重写为**结构化、确定性(按 symbol 播种)、带真实感硬数字**的 9 章报告(`## 摘要/业务画像/财务诊断/交叉验证/风险量化/估值建模/逆向视角/投资建议/附录`),全部标注"模拟数据",数字由 symbol 派生(营收/ROE/PE/PB/目标价/评级)。字数 275→约6200,渲染出 TOC+9 分节,无误标 v2 降级横幅。
- 测试:`tests/test_report_structure.py` 新增 `TestMockReport`(断言含 `## ` 章节、标注"模拟"、无"主营业务涵盖多个领域"空话、渲染出 TOC+≥8 分节)。
- 校验:ruff/mypy 通过(修复 E501 超长行,改 `\n\n`.join 短句拼接);pytest 全绿(报告结构套件 8 passed;离线套件 19 passed)。
- 待用户确认:是否要跑一次**真实 v2**(有 Key)产出真实分析报告;真实 v2 内容质量已通过 7/6 格力缓存佐证为良好。

## 任务6：真实报告"没有表格"根因修复
- 用户跑真实 v2(`aimoon 600519`,17:24 出报告 `600519_20260707_172433.html`)反馈"没有表格"。诊断:
  - AI 正文质量本身很好(硬数字/SWOT/CAGR/ROE杜邦/风险量化/估值建模俱在),ai-section 内已有 6 个 `<table>`(AI 自己写的 SWOT/比率表等渲染正常)。
  - **核心 bug**:compile.md 承诺"财务时序表/同行竞品对比表/估值三档表已由系统预渲染",但 `table_renderer.py` 算出的 `tables_md` 只被塞进 **ANALYSIS 阶段 prompt**(orchestrator.py:269),**没写回 `prior`**;COMPILE 阶段既没拿到 `tables_md`(phase_system_prompt 无该占位符),终稿 `final_markdown` 三处落定点(use_fast/compile成功/compile回退)也从未把表追加上去 → 三张核心表算出来却被丢弃,AI 只能以"模型训练数据"硬编同业数字。
- 修复(orchestrator.py + phases.py + compile.md):
  1. `_phase_analysis` 把 `tables_md` 写入 `prior`(=prior["tables_md"]=tables_md)。
  2. 新增静态方法 `_append_system_tables(md, prior)`:把三张表标题由 `##` 降为 `###`、包进统一 `## 系统预渲染数据表（aimoon 自动附加）` 章节,在 `final_markdown` 三处赋值点全部追加。**守卫不能用字面串"系统预渲染数据表"**(COMPILE prompt 自己就提这个词,AI 正文一提就误判"已追加"而跳过→表消失);改为判断真实表头 `| 报告期 | 营收(亿)` 是否已存在才跳过。此守卫 bug 在第一版实现时已踩坑并修正。
  3. `phase_system_prompt` 增加 `{{ tables_md }}` 注入;`compile.md` 增加 `{{ tables_md }}` 占位符,使 COMPILE 能引用精确数字、"系统预渲染"声明在上下文为真。
- 回归测试:新增 `tests/test_pipeline_tables.py`(5 项)_assert 追加后含 `## 系统预渲染数据表`+3 个 `### ` 表+`<table>` 分隔行;渲染后 ai-section 内 `<table>`≥3;COMPILE prompt 注入 `tables_md`;追加幂等/空表 noop。
- 校验:ruff/mypy 通过(orchestrator+phases);pytest `test_pipeline_tables.py` 5 passed。
- 真实重跑已后台启动(task 8flkfQ),完成后确认带表报告。

## 任务7/8(本轮回合)：用户反馈"显示已降级 + 排版不清" → 根因修复 + 清晰度打磨
- **"已降级"误报根因**：`generator.py` 旧逻辑用 `("⚠️" in report_text) or ("降级" in report_text)` 嗅探正文。但 COMPILE prompt 强制要求每份正常报告末尾带 `> ⚠️ 本报告由 AI 自动生成...` 风险提示 → 每份报告都含 ⚠️ → `degraded` 永远 True,**误报"已降级"**(且旧 badge-down 是绿色,更误导)。
- **修复(改为显式布尔标志)**：
  1. `analysis_report.py:AnalysisReport` 新增 `degraded: bool = False` 与 `system_tables_md: str = ""` 两个字段。
  2. `analyzer.py:_mark_degraded` 改为 `model_copy(update={"report_text": ...+marker, "degraded": True})`(不再只靠文本嗅探)。
  3. `generator.py:_build_context` 的 `report_meta["degraded"]` 改为 `bool(getattr(analysis,"degraded",False))`;`index.html` 降级 badge 由 `badge-down`(绿)→`badge-warn`(琥珀),并加 `report-degraded` 横幅。
- **系统三表改为独立「数据附录」卡片区**(不再塞进 AI 长文末尾、不依赖 AI 是否粘贴)：
  1. `orchestrator.py`：`PipelineContext` 新增 `system_tables_md`;`_phase_analysis` 末尾 `ctx.system_tables_md = str(prior.get("tables_md") or "")`;**删除**已废弃的 `_append_system_tables` 静态方法(三处调用改为纯 `ctx.final_markdown = ...`)。
  2. `analyzer.py:_build_report` 接收并传入 `system_tables_md`;`_pipeline_analyze` 从 `ctx` 取出传给 `_build_report`。
  3. `generator.py` 新增 `_render_system_tables(md)`：按 `## ` 拆三张子表为独立 `.sys-table-card`(缺失子表自动跳过),返回 `.sys-tables` 区块;`_build_context` 注入 `system_tables_html`。`index.html` 在 AI 报告卡片后新增「系统数据附录」卡片(`badge-up`"系统计算·非AI生成",说明供交叉核对)。
- **排版清晰度打磨**(`style.css` + `index.html`)：AI 表格 `display:block;overflow-x:auto` + 斑马纹 `tbody tr:nth-child(even)` + `th` 白字加粗;`ai-section + ai-section` 间距加大(32px)、`h2` 加下边框 + flex 对齐;新增 `.sys-appendix`(蓝色边框)/`.sys-tables`/`.sys-table-card`/`.sys-table-title`/`.sys-table-body` 并配 light 模式覆盖。
- **测试重写** `tests/test_pipeline_tables.py`:旧 `TestSystemTablesReachReport` 引用已删的 `_append_system_tables`(运行必崩)→ 改为：
  - `test_system_tables_render_as_appendix_cards`：构造带 `system_tables_md` 的 `AnalysisReport`→`HtmlReportGenerator` 生成→断言 `.sys-tables`/`.sys-table-card` + `<th>报告期/公司/档位</th>` 真实渲染,且不在 `ai-section` 内。
  - `test_no_system_tables_when_empty`：无 `system_tables_md` 时不渲染附录。
  - `test_compile_prompt_injects_tables_md` 保留(COMPILE prompt 仍注入 `tables_md` 供 AI 引用)。
  - 新增 `TestDegradedFlagIsExplicit`：`test_normal_report_with_risk_disclaimer_not_degraded`(正文含 ⚠️ 风险提示但 `degraded=False` → 不显示"已降级"/"正常"badge/`class="report-degraded"` 横幅)**直接守护原 bug**;`test_degraded_report_flagged`(`_mark_degraded` 置 True → 显示"已降级"+横幅);`test_mark_degraded_returns_new_report_and_preserves_text`(不可变:frozen model 原对象不变、新副本带标志+marker)。
- **校验**：ruff(5 文件全过)+ mypy(orchestrator/analyzer/generator 全过);pytest `test_pipeline_tables.py` 6 passed + `test_valuation_targets.py` 全过。临时审计脚本 `report_audit.py`/`report_audit2.py`/`val_check.py` 已清理。
- **端到端真实重跑** `aimoon 600519 -o output` 后台启动(task QpIc0D),完成后审计新报告:应无误报"已降级"、系统三表在独立「系统数据附录」、排版清晰。

## 任务13/14/15(续前)：用户再反馈"末尾风险提示重复 + 排版还是很丑"
- **免责声明重复根因**：模板页脚(`index.html` 的 `.disclaimer`)已统一渲染免责声明;但 `compile.md` 又**强制 AI 在末段写** `> ⚠️ 本报告由 AI 自动生成...` → 正文一遍 + 页脚一遍。更糟:AI 偶发把同一句写两遍、中间夹字面 `>` → markdown 渲染出裸 `&gt;` 字符,即用户看到的"重复最后一段"。
- **修复(双保险)**：
  1. `compile.md`:删除"末段强制 risk disclaimer"指令(模板页脚才是唯一来源);并新增"每小节用空行分 2-4 自然段、勿整节一大段"的排版约束。
  2. `generator.py`:新增 `_DISCLAIMER_RE` + `_strip_disclaimer(md)`:贪婪匹配 `(?:>\s*)?⚠️?\s*本报告由[\s\S]*(?:自担风险|投资建议|入市需谨慎)[。.]?`,在 `_build_context` 渲染前对 `report_text` 剥离。兼容:真实版/精简版/mock 版/双写+`&gt;` 四种形态;**降级提示**(`> ⚠️ 本次分析部分阶段降级`,不含"本报告由")不被误删。
- **blockquote 裸文本修复**(`style.css`):`.ai-section` 此前只有 `.ai-report blockquote` 样式,`.ai-section blockquote` 无基础样式 → AI 正文引用块渲染成裸文本。补齐 `.ai-section blockquote`(左边框+琥珀底+圆角),与 `.ai-report` 一致。
- **墙状文字对齐**(`style.css`):`.ai-section p` 加 `text-align: justify; text-justify: inter-ideograph; word-break: break-word;`(配合既有 `text-indent: 2em` + `line-height:1.85`),消除长段落右边缘参差。
- **测试**:`tests/test_pipeline_tables.py` 新增 `TestDisclaimerNotDuplicated`(4 项:双写剥离保留真实内容 / mock 形态清空 / 降级提示保留 / 渲染后仅页脚 1 份"本报告由AI自动生成"且 ai-section 内无"本报告由")。顺手修了重复的 `FinancialData` import。
- **校验**:ruff(全过)+ mypy(generator 全过);pytest `test_pipeline_tables.py` 13 passed。AI 分析缓存已清(迫使新 compile.md prompt 生效),财务缓存保留(三大表已新鲜),真实重跑 `aimoon 600519 -o output` 后台(task BPfk1K),完成后审计:仅页脚 1 份免责声明、无 `&gt;`、段落更短、引用块有样式。

## 任务16(本回合收尾)：报告深度优化(9章重构 + 3 计算工具)最终校验
- 改动清单(前序 session 完成,本轮仅收尾校验):
  - `phases.py`:`_REPORT_SECTIONS` 重构为 9 章总-分-总(核心结论/关键数据一览/行业背景与政策环境/业务画像与护城河/财务健康诊断/多方信号验证与风险矩阵/估值建模与情景分析/投资建议与操作计划/附录)。
  - 新增 3 个纯函数工具:`fcf_dividend.py`(自由现金流/股息/FCF覆盖分红)、`scenario_prob.py`(情景概率加权/风险收益比)、`sentiment.py`(社媒情感+高频词,SnowNLP 优先+金融词典兜底)。均注册进 `tools/__init__.py` 的 `TOOL_RUNNERS`。
  - `financial_temporal.py`:`_serialize` 新增 investing_cf/financing_cf/total_assets/total_liabilities/equity 供 capex 计算。
  - `table_renderer.py`:新增 `render_fcf_dividend`/`render_scenario_prob`/`render_sentiment` 3 渲染函数 + `_fmt_yi_amount`(三大表金额统一"亿元");三大表明细金额统一 ÷1e8。
  - `orchestrator.py`:并行跑 sentiment(波1)/fcf_dividend+scenario_prob(波2);summary 注入 user message;`_extract_tool_summary` 补新工具。
  - `prompts/analysis.md`/`compile.md`/`self_check.md` + `prompts.py` legacy:全文同步 9 章结构与量化要求(FCF穿透/情景概率加权/机构分歧/可逆反转阈值/风险收益比/关键假设+敏感性/催化剂日历/数据可信度/单位统一)。
  - `generator.py` + `style.css`:`_render_system_tables` 为每卡片追加 `_credibility_for()` 数据可信度脚注;新增 `.sys-table-cred` 样式(含 light 模式)。
  - `pyproject.toml`:新增 `nlp = ["snownlp>=0.12.0","jieba>=0.42.1"]` optional;3 新工具文件加 E501 豁免。
- 修复的历史 bug(前序 session):情景渲染 `+-53.0%` 符号错→`{down:+.1f}%`/`{up:+.1f}%`;三大表金额原"元"混用→`_fmt_yi_amount` 统一"亿元"(应收账款 2609048.5 元→0.03 亿);mock history 顺序(最新在前)。FCF 覆盖分红 0.47 倍正确标"⚠️ 不可持续"。
- **本轮最终校验**:全项目 `ruff check src/` → **All checks passed!**(用全局 `/c/Users/Administrator/.local/bin/ruff` 直跑,避开 `uv run` 环境解析卡顿);临时 `smoke_test_tools.py` 已删除。3 工具文件/3 registry 项/3 渲染函数/9 章结构均确认在位。
- TaskCreate #1–#7 全部 completed。

## 运维发现：`uv run aimoon` 卡死根因 + 绕过方案（重要）
- **现象**：`uv run aimoon <code>` 长时间(7min+)无输出,`ps` 中只见 `uv.exe run` 进程、无任何 python/aimoon 子进程 → 卡在 `uv` 环境同步(联网拉包/重锁),根本没启动业务代码。
- **根因**:`uv run`(不带 `--no-sync`)每次都会重新解析/同步 venv 环境;本机到包索引的网络解析极慢/挂起,导致启动无限期卡住。
- **验证**:`uv run --no-sync ruff --version` 与 `uv run --no-sync aimoon --help` 均**秒回**,证明加 `--no-sync` 跳过同步即可正常启动(`.venv` 已含全部依赖,来自历史 `uv sync` / `pip install -e .`)。
- **全局 `aimoon` 二进制是坏的**:`/c/Users/Administrator/.local/bin/aimoon` 指向旧版入口 `aimoon.main:main`(ModuleNotFoundError),**不可用**。正式运行一律用 `uv run --no-sync aimoon`。
- **遗留僵尸进程**:`DeferExecuteTool TaskStop` 报告 "killed" 但 `uv` 子进程常残留(如 Al7pVk 的 `uv run ruff` 空转 24min)。需用 `kill -9 <pid>` 直接杀 `uv.exe` 进程。
- **结论/约定**:本项目跑 aimoon 统一用 `uv run --no-sync aimoon <code> -o output`,日志重定向到文件(勿用 `| tail`,会吞掉流式进度);监控用 `tail` 日志文件或读 `/tmp/aimoon_*.log`。
- **端到端验证**:`uv run --no-sync aimoon 000651 -o output` 成功生成 `output/000651_20260707_211348.html`(122KB)。核验:9 章全在、系统数据附录卡片(sys-tables/sys-table-card)、可信度脚注(sys-table-cred/数据可信度)、页脚免责声明仅 1 份(正文重复已被剥离)。真实 v2 报告质量良好(含硬数字:格力 38.4/-1.54%/PE7.37;情景:保守48.30元/31.1%、中性/乐观;评级买入、目标价54.95元)。

## 任务8–11(本轮回合)：报告排版 + 文字质量优化(用户 /using-superpowers 触发)
- 用户痛点:滥用代码块(```)、段落过长(200-300字"文字墙")、关键数字不突出、AI 废话词/角色扮演、表格"点评"列密集。
- **generator.py 新增 3+1 后处理函数(渲染前 `_postprocess_report` 串联)**:
  1. `_strip_code_fences(md)`:删 ``` 围栏行,内容转普通段落(安全网;prompt 已禁止)。
  2. `_strip_roleplay(md)`:删"好的,分析师…"开头 + "作为(您的)首席分析师"内联。正则边界化到首个句末标点(。！？),避免误删同行真实内容。
  3. `_highlight_key_numbers(md)`:PE/PB/ROE/股息率/OCF净利比/目标价/现价 包裹 `<span class="highlight">`(bleach 已允许 span+class)。分隔符支持半角 `=`/`:` 与**全角 `：`**(中文常见,初版漏掉导致只高亮 6 处,补全后 14→9 处)。
  4. `_split_long_paragraphs(md, limit=150)`:超长段落按句末标点(。！？)最近段中处硬拆,缓解"文字墙";跳过 #/-/*/>/|/数字. 开头块(列表/表格/标题不拆),不切断 highlight span/加粗。
- **style.css 新增**:`.highlight`(黄底渐变+加粗,light 模式琥珀底)、`pre/code` 兜底 `white-space:pre-wrap`(防残余横向滚动)、`.ai-section p { max-width:42em }` 限制行宽(移动端 100%)。
- **Prompt 同步(排版与文字风格硬约束)**:
  - `compile.md`:新增「排版禁律 / 华尔街投行备忘录文风 / 每章三模块(核心结论→证据→风险) / 情景分析直接明确」章节;改"最终约束"——明文**严禁代码块**、段落 ≤5 行且首句加粗、关键数字加粗、表格不设长点评列(结论放表格下方 > 引用块"结论气泡")。
  - `analysis.md`:关键规则补「排版」(禁 fences/段落≤5行/数字加粗/表格轻量化)与「文字」(禁废话词与角色扮演开头)。
  - `self_check.md`:新增维度 6「排版合规」(代码块/巨型段落/数字加粗/点评列)、7「文字干净度」(废话词/角色扮演);最多修复项 5→7。
  - `prompts.py` legacy SYSTEM_PROMPT:表达规则补一致约束。
- **Bug 修复**:初版 `_wrap` 用 `m.group(3)`,但 OCF 正则仅 2 组 → `IndexError: no such group` 致整份报告生成失败(REPORT_EXIT=1)。改为 `m.groups()` 取单元,`unit or ""` 兜底。实测 AI 用全角冒号写"目标价：54.95元"→ 分隔符加 `：` 后命中。
- **验证(终版 `output/000651_20260707_214414.html`,114KB)**:代码块 0、角色扮演 0、废话词 0、highlight 9、结论气泡 blockquote 3、9 章、免责声明 1 份;段落平均 78 字(原 112)、最长 279(原 376)、仅 2 段 >150。ruff 全项目 All checks passed。临时 `test_transforms.py` 已删。
- TaskCreate #8–#11 全部 completed。

## 任务 E(本回合)：正文"窄列换行/一行几个字"根因修复
- **用户现象**：报告正文"文字还没写到末尾就开始换行，一行才几个字"。实测定位(Playwright 量渲染宽度)：viewport 1280px 下 `.container` 宽 1280px，但 `#sec-1` 段落实测仅 **588px**（约 42 字/行），右侧空出近 700px → 视觉上"文字没到页边就换行"。
- **根因**：`style.css`(报告模板) `.ai-section p { max-width: 42em }`（Phase C 任务8–11 加的"限制行宽"规则，42em≈588px @14px）把正文卡死在 588px 窄列。Task #8–#11 日志 line 127 记了这条规则，现证明确实导致窄列+右侧留白。
- **修复**：`style.css` 该规则改为 `.ai-section p { max-width: 100% }`（取消行宽上限，正文撑满容器内容区）；删冗余 `@media(max-width:768px)` 覆盖。同步把已生成报告 `output/000651_20260707_215434.html` 的 inline `<style>` 同一处 `max-width:42em`→`100%`，让用户即时预览。
- **复测**：viewport 1280→段宽 1094px(约 78 字/行)、900→714px(51)、480→398px(28)，文字现已写到页边、右侧留白消失。
- **附带的仍存在的问题(用户贴文中可见，本次未改)**：① 首句整段加粗(`<strong>投资评级：买入。…表象。</strong>` 粗体墙，违反 Phase D「禁整段加粗」规则，但本次重跑 LLM 仍输出)；② 悬空角标 `近5年历史最低10%分位¹⁾`（违反 Phase D「角标须用"注:"落地」规则，LLM 仍输出）。二者属 LLM 内容层、非 CSS，需更强约束或生成器后处理；已向用户说明并提议用确定性后处理兜底(不依赖 LLM 守规则)。
- **清理**：临时 `measure_report.py` 已删。

## 任务 F(本回合)：加确定性后处理兜底「粗体墙 + 悬空角标」(用户回"加")
- **动机**：Phase D 已在 prompt 加了「禁整段加粗 / 角标须用"注:"落地」规则,但本次重跑 LLM 仍输出 `<strong>投资评级：买入。…机会。</strong>`(整句加粗墙) 与 `近5年历史最低10%分位¹⁾`(悬空上标角标) → prompt 约束管不住 LLM,改用具确定性后处理兜底(不依赖 LLM 自觉)。
- **generator.py 新增两函数并接入 `_postprocess_report`**(顺序:去代码块→清角色扮演→**拆粗体墙**→高亮数字→拆长段→**清悬空角标**):
  1. `_fix_bold_walls(md)`:正则 `\*\*([\s\S]*?[。！？][\s\S]*?)\*\*` 命中「`**` 内含句末标点`(。！？)`」的整句加粗 → 仅在首个句末标点处保留 `**`,其后还原普通文本。关键数字 `**7.37**` 不含句末标点不受影响;短标签 `**关键数字锚点**：` 不受影响。
  2. `_strip_dangling_footnotes(md)`:删上标数字角标 `[\u2070\u00B9\u00B2\u00B3\u2074-\u2079]+[\u207D\u207E]?`(如 `¹⁾`/`²`)。守卫:`_FN_DEFINED_RE` 检测正文已有「注：…①」定义块(含上标/带圈数字)则不动;否则删除。内联来源(`分位¹⁾(来源:年报)`)也清标记留来源。
- **验证**:`probe_fn.py` 逐条打印断言全 True(首句加粗保留/`**7.37**` 保留/`¹⁾` 清除/已定义不误删);新增 `tests/test_report_transforms.py`(9 项:粗体墙 5 + 角标 4)。`ruff check src/` All checks passed。本机 `.venv` 无 dev 依赖致 `pytest` 不可跑(避免 `uv sync` 卡死),用例留待 CI。
- **真实重跑**:`uv run --no-sync aimoon 000651 -o output` 后台(task C1l9RT, log `/tmp/aimoon_000651_v7.log`),完成后核验新报告粗体墙/悬空角标消失。临时 `probe_fn.py` 已删。
